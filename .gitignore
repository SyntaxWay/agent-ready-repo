# Dependencies
node_modules/

# Build outputs
dist/
.turbo/

# Environment files — never commit secrets
.env
.env.local
.env.*.local

# Claude Code personal overrides — project settings.json IS committed; local overrides are not
.claude/settings.local.json

# OS & editor
.DS_Store
*.swp
